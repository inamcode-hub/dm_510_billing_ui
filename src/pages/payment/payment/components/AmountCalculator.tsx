import React from 'react';

const AmountCalculator: React.FC = () => {
  //   const { country, province } = useSelector((state: any) => state.payment);

  return (
    <React.Fragment>
      <div>
        <h1>AmountCalculator</h1>
      </div>
    </React.Fragment>
  );
};

export default AmountCalculator;
